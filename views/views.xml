<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="kedamos.list">
      <field name="name">kedamos list</field>
      <field name="model">kedamos.kedamos</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="kedamos.action_window">
      <field name="name">kedamos window</field>
      <field name="res_model">kedamos.kedamos</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="kedamos.action_server">
      <field name="name">kedamos server</field>
      <field name="model_id" ref="model_kedamos_kedamos"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="kedamos" id="kedamos.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="kedamos.menu_1" parent="kedamos.menu_root"/>
    <menuitem name="Menu 2" id="kedamos.menu_2" parent="kedamos.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="kedamos.menu_1_list" parent="kedamos.menu_1"
              action="kedamos.action_window"/>
    <menuitem name="Server to list" id="kedamos" parent="kedamos.menu_2"
              action="kedamos.action_server"/>
    -->
  </data>
</odoo>